<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tapperware.techera">

    <application
        android:allowBackup="true"
        android:icon="@drawable/teraicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/teraicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActvity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Profile" />
        <activity android:name=".About" />
        <activity android:name=".Start" />
        <activity android:name=".RecycleViewDomestic" />
        <activity android:name=".RecycleViewOverseas" />
        <activity android:name=".DetailTech"></activity>
    </application>

</manifest>